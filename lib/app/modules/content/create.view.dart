import 'package:flutter/material.dart';

class CreateContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Página formularios criar novo conteúdo'),
      ),
    );
  }
}